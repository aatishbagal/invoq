llm:
  backend: "ollama"       # ollama | lmstudio
  model: "auto"           # Ollama auto-selection, or an explicit model identifier
  api_url: null           # Use the selected backend's default URL

security:
  remediation_mode: true

extensions:
  enabled: []
